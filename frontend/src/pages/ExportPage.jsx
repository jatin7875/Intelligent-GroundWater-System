import { useState } from 'react';
import { Download, FileJson, FileSpreadsheet, FileText } from 'lucide-react';
import { toast } from 'sonner';
import { Breadcrumb, PageHeader } from '../components/layout/PortalLayout';
import { stations } from '../data/mockData';

export default function ExportPage(){const [history,setHistory]=useState([{name:'nagpur_station_readings.csv',type:'CSV',size:'42 KB',date:'14 Jul 2026'}]);const generate=(type)=>{const row={name:`jaldrishti_export_${Date.now()}.${type.toLowerCase()}`,type,size:type==='JSON'?'58 KB':'42 KB',date:'14 Jul 2026'};setHistory([row,...history]);toast.success(`${type} demonstration export generated.`)};return <><Breadcrumb items={['Researcher','Data Export']}/><PageHeader title="Data Export" description="Prepare filtered station, reading, recharge, forecast, and metadata datasets."/><section className="page-section"><div className="container content-grid"><section className="panel"><h2>Configure export</h2><div className="form-grid"><div className="field"><label>Data type</label><select><option>Cleaned readings</option><option>Raw readings</option><option>Gap-filled readings</option><option>Recharge calculations</option><option>Forecast data</option><option>Station metadata</option></select></div><div className="field"><label>Station</label><select><option>All stations</option>{stations.slice(0,8).map(s=><option key={s.id}>{s.name}</option>)}</select></div><div className="field"><label>State</label><select><option>All states</option><option>Maharashtra</option></select></div><div className="field"><label>Date range</label><select><option>Last 12 months</option><option>All available</option></select></div></div><div className="export-formats"><button onClick={()=>generate('CSV')}><FileSpreadsheet/>CSV<small>Spreadsheet-ready</small></button><button onClick={()=>generate('JSON')}><FileJson/>JSON<small>Machine-readable</small></button><button onClick={()=>toast.info('PDF summary uses the report builder.')}><FileText/>PDF summary<small>Printable report</small></button></div></section><aside className="panel"><h2>Export notes</h2><p>All files are labelled as demonstration data. The production service endpoint can replace the mock generator without page changes.</p><p>Dates use ISO 8601 and water levels use metres below ground level (m bgl).</p></aside></div><div className="container"><div className="section-head dashboard-section"><div><h2>Export history</h2></div></div><div className="data-table-wrap"><table className="data-table"><thead><tr><th>File name</th><th>Type</th><th>Size</th><th>Generated</th><th>Action</th></tr></thead><tbody>{history.map((item,index)=><tr key={`${item.name}-${index}`}><td>{item.name}</td><td>{item.type}</td><td>{item.size}</td><td>{item.date}</td><td><button className="text-button" onClick={()=>toast.success('Download started (demonstration).')}><Download size={15}/> Download</button></td></tr>)}</tbody></table></div></div></section></>}
